/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["localhost", "m.media-amazon.com", "via.placeholder.com", "i.gr-assets.com"],
    },
};

module.exports = nextConfig;
